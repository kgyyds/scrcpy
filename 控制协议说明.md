# scrcpy 控制流与控制协议说明（PC `app/` → Android `server/`）

本文基于仓库当前实现，梳理 **PC 端如何把控制事件发送到 Android 端（jar/server）**，并对二进制控制协议做逐项说明。

---

## 1. 控制链路总览

## 1.1 PC 侧（`app/`）链路

1. 用户在 PC 上产生输入（键盘/鼠标/触摸板）后，`input_manager` / `mouse_sdk` 组装 `sc_control_msg`。
2. `sc_controller_push_msg()` 将消息入队。
3. `sc_controller` 线程从队列取消息，调用 `sc_control_msg_serialize()` 序列化成二进制。
4. 通过 `net_send_all(control_socket, ...)` 发送到控制 socket。

对应代码位置：
- 输入消息构造：`app/src/input_manager.c`、`app/src/mouse_sdk.c`
- 控制消息定义与序列化：`app/src/control_msg.h`、`app/src/control_msg.c`
- 队列与发送线程：`app/src/controller.c`

## 1.2 Android 侧（`server/`）链路

1. `Server` 创建 `Controller`，`Controller` 持有 `ControlChannel`。
2. `ControlChannel.recv()` 使用 `ControlMessageReader.read()` 按协议解析字节流。
3. `Controller.handleEvent()` 根据 `msg.type` 分发。
4. 通过 `Device.injectKeyEvent()` / `Device.injectEvent()` 触发系统输入注入。

对应代码位置：
- 控制通道入口：`server/src/main/java/com/genymobile/scrcpy/control/ControlChannel.java`
- 协议解析：`server/src/main/java/com/genymobile/scrcpy/control/ControlMessageReader.java`
- 事件处理与注入：`server/src/main/java/com/genymobile/scrcpy/control/Controller.java`

---

## 2. 控制消息类型（type）

`type` 占 1 字节，枚举值（PC/Android 两端保持一致）：

| type 值 | 含义 |
|---:|---|
| 0 | INJECT_KEYCODE |
| 1 | INJECT_TEXT |
| 2 | INJECT_TOUCH_EVENT |
| 3 | INJECT_SCROLL_EVENT |
| 4 | BACK_OR_SCREEN_ON |
| 5 | EXPAND_NOTIFICATION_PANEL |
| 6 | EXPAND_SETTINGS_PANEL |
| 7 | COLLAPSE_PANELS |
| 8 | GET_CLIPBOARD |
| 9 | SET_CLIPBOARD |
| 10 | SET_DISPLAY_POWER |
| 11 | ROTATE_DEVICE |
| 12 | UHID_CREATE |
| 13 | UHID_INPUT |
| 14 | UHID_DESTROY |
| 15 | OPEN_HARD_KEYBOARD_SETTINGS |
| 16 | START_APP |
| 17 | RESET_VIDEO |

---

## 3. 二进制协议字段说明（重点消息）

> 字节序：整型统一为 **大端（big-endian）**。  
> 文本编码：UTF-8。  
> 最大消息大小：256 KiB。

## 3.1 `INJECT_KEYCODE`（type=0）

格式（总长 14 字节）：

- `type`：1B
- `action`：1B（`AKEY_EVENT_ACTION_DOWN=0`，`UP=1`）
- `keycode`：4B（Android KeyEvent keycode）
- `repeat`：4B
- `metastate`：4B（修饰键状态）

用途：电源键、音量键、Home、Back、AppSwitch、Menu 等都走这个消息。

## 3.2 `INJECT_TOUCH_EVENT`（type=2）

格式（总长 32 字节）：

- `type`：1B
- `action`：1B（DOWN/UP/MOVE/HOVER_MOVE 等 MotionEvent action）
- `pointer_id`：8B
- `position.x`：4B
- `position.y`：4B
- `position.screen_width`：2B
- `position.screen_height`：2B
- `pressure`：2B（u16 定点）
- `action_button`：4B（鼠标按钮）
- `buttons`：4B（当前按钮状态）

`pointer_id` 里有几个约定值：
- `-1`：mouse
- `-2`：generic finger
- `-3`：virtual finger（用于某些手势模拟）

## 3.3 `INJECT_SCROLL_EVENT`（type=3）

格式（总长 21 字节）：

- `type`：1B
- `position(x,y,w,h)`：12B
- `hscroll`：2B（i16 定点）
- `vscroll`：2B（i16 定点）
- `buttons`：4B

滚动值在 PC 侧接受范围 `[-16,16]`，先归一化到 `[-1,1]` 编码；Android 解析后再乘回 16。

## 3.4 `BACK_OR_SCREEN_ON`（type=4）

格式（总长 2 字节）：

- `type`：1B
- `action`：1B

语义：
- 屏幕亮：注入 BACK
- 屏幕灭：仅在 ACTION_DOWN 时注入 POWER（点亮）

---

## 4. 你关心的场景逐项说明

## 4.1 滑动屏幕（拖动/手指移动）发送什么？

典型是连续 `INJECT_TOUCH_EVENT(type=2)`：

1. 按下起点：`action=DOWN`、`pressure≈1.0`
2. 轨迹移动：多次 `action=MOVE`
3. 抬起终点：`action=UP`、`pressure=0.0`

同一次滑动通常 `pointer_id` 保持一致。Android 端在 `Controller.injectTouch()` 中维护多指状态，必要时会把第二根及以上手指转成 `ACTION_POINTER_DOWN/UP | index<<shift`。

---

## 4.2 点击屏幕发送什么？

点击本质是简化版触摸序列，依然是 `INJECT_TOUCH_EVENT(type=2)`：

- 一次 DOWN
- 一次 UP

若是鼠标左键点击，会带：
- `pointer_id = -1`（mouse）
- `action_button = BUTTON_PRIMARY`
- `buttons` 在 DOWN 时包含主键位，在 UP 时清零

Android 端会根据 `pointer_id` 与按钮状态判定是 mouse source 还是 touch source。

---

## 4.3 按下电源键发送什么？

发送 `INJECT_KEYCODE(type=0)`，关键字段：

- `action=DOWN(0)` 或 `UP(1)`
- `keycode=AKEYCODE_POWER(26)`
- `repeat=0`
- `metastate=0`

也就是说，按住/释放会分别发一条 keycode 消息。

示例（DOWN，14 字节）：

- `type=0x00`
- `action=0x00`
- `keycode=0x00 0x00 0x00 0x1A`（26）
- `repeat=0x00 0x00 0x00 0x00`
- `metastate=0x00 0x00 0x00 0x00`

---

## 4.4 音量键发送什么？

同样是 `INJECT_KEYCODE(type=0)`：

- 音量加：`keycode=AKEYCODE_VOLUME_UP(24)`
- 音量减：`keycode=AKEYCODE_VOLUME_DOWN(25)`
- `action` 区分按下/抬起

示例（音量+ DOWN）：
- `type=0x00`
- `action=0x00`
- `keycode=0x00 0x00 0x00 0x18`（24）
- `repeat=0`
- `metastate=0`

示例（音量- DOWN）：
- `keycode=0x00 0x00 0x00 0x19`（25）

---

## 4.5 鼠标滚轮发送什么？

发送 `INJECT_SCROLL_EVENT(type=3)`：

- 携带当前位置 `position`
- `hscroll/vscroll` 为滚动增量（协议定点编码）
- `buttons` 记录滚动时按下的鼠标按钮状态

Android 端转换为 `MotionEvent.ACTION_SCROLL`，并把值写到 `AXIS_HSCROLL / AXIS_VSCROLL`。

---

## 5. PC 快捷键到协议的关系（示例）

在默认快捷键路径中：

- `MOD + p` → `action_power()` → `INJECT_KEYCODE(keycode=POWER)`
- `MOD + ↑` → `action_volume_up()` → `INJECT_KEYCODE(keycode=VOLUME_UP)`
- `MOD + ↓` → `action_volume_down()` → `INJECT_KEYCODE(keycode=VOLUME_DOWN)`

这些消息最终都由 `sc_controller` 序列化并发往控制 socket。

---

## 6. Android 端注入策略补充

1. **按键类（无坐标）**：走 `Device.injectKeyEvent()`，目标 display 通过 `getActionDisplayId()` 选择。  
2. **触摸/鼠标类（有坐标）**：先用 `PositionMapper` 把视频坐标映射到设备坐标，再注入 `MotionEvent`。  
3. **BACK_OR_SCREEN_ON**：如果屏幕关闭，`ACTION_DOWN` 不发 BACK，而是发一次 POWER（press-release）。

---

## 7. 与测试用例对照（验证协议字节布局）

`app/tests/test_control_msg_serialize.c` 已覆盖并验证：

- keycode 序列化长度/字节
- touch 序列化长度/字节
- scroll 序列化长度/字节
- back_or_screen_on 序列化长度/字节

可将该文件视为协议“字节级真值”参考。

---

## 8. 小结

- scrcpy 控制协议是 **type + payload** 的定长/变长二进制帧，PC 与 Android 两端严格镜像实现。  
- 你关心的 **滑动、点击、Power、Volume** 统一可归结为：
  - 滑动/点击：`INJECT_TOUCH_EVENT (type=2)`
  - Power/Volume：`INJECT_KEYCODE (type=0)`
- Android 端最终都收敛到系统输入注入（`KeyEvent`/`MotionEvent`）。

