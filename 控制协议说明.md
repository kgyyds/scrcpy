# Kotlin 客户端对接 scrcpy server（jar）控制链路与控制协议完整实现说明

> 目标：你已经在 Android App 内实现了视频/音频播放，现在需要把“用户在渲染画面上的输入（点击、滑动、长按、按键）”转换为 scrcpy 控制协议字节流，发送给 jar 端（`server/`）并完成设备控制。

本文按“**可以直接落地实现**”来写，包含：链路、协议、坐标换算、事件时序、Kotlin 序列化与发送代码模板。

---

## 1. 端到端控制链路（你要实现的部分）

scrcpy 官方 PC 端链路是：

1. 输入事件构造成 `sc_control_msg`
2. 入队
3. 序列化为二进制
4. 写入 control socket
5. Android jar 端 `ControlMessageReader` 解析
6. `Controller` 注入 `KeyEvent` / `MotionEvent`

你在 Kotlin App 里要做的就是实现同等逻辑：

- **采集 UI 事件**（视频渲染 View 上的 touch/mouse）
- **映射坐标**（View 坐标 -> 视频坐标 -> 设备坐标信息）
- **按协议编码**（与 `app/src/control_msg.c` 完全一致）
- **发往 control 通道**（TCP/Socket 输出流）

---

## 2. 协议事实来源（必须对齐）

协议定义与解析来自以下源码（建议你实现时对着写）：

- PC 侧消息类型与字段：`app/src/control_msg.h`
- PC 侧二进制序列化：`app/src/control_msg.c`
- Android 侧 type 常量：`server/.../control/ControlMessage.java`
- Android 侧二进制解析：`server/.../control/ControlMessageReader.java`
- Android 侧注入行为：`server/.../control/Controller.java`
- 字节级样例：`app/tests/test_control_msg_serialize.c`

---

## 3. 控制消息 type（1 字节）

| 值 | 含义 |
|---:|---|
| 0 | INJECT_KEYCODE |
| 1 | INJECT_TEXT |
| 2 | INJECT_TOUCH_EVENT |
| 3 | INJECT_SCROLL_EVENT |
| 4 | BACK_OR_SCREEN_ON |
| 5 | EXPAND_NOTIFICATION_PANEL |
| 6 | EXPAND_SETTINGS_PANEL |
| 7 | COLLAPSE_PANELS |
| 8 | GET_CLIPBOARD |
| 9 | SET_CLIPBOARD |
| 10 | SET_DISPLAY_POWER |
| 11 | ROTATE_DEVICE |
| 12 | UHID_CREATE |
| 13 | UHID_INPUT |
| 14 | UHID_DESTROY |
| 15 | OPEN_HARD_KEYBOARD_SETTINGS |
| 16 | START_APP |
| 17 | RESET_VIDEO |

---

## 4. 你当前最需要的 4 类消息

## 4.1 点击/滑动/长按：`INJECT_TOUCH_EVENT`（type=2）

固定 32 字节：

- type: 1B
- action: 1B
- pointerId: 8B
- x: 4B
- y: 4B
- screenWidth: 2B
- screenHeight: 2B
- pressure: 2B（u16 定点）
- actionButton: 4B
- buttons: 4B

### action 关键值

- DOWN = 0
- UP = 1
- MOVE = 2
- HOVER_MOVE = 7（可选）

### 事件序列

- 点击：DOWN -> UP
- 滑动：DOWN -> MOVE... -> UP
- 长按：DOWN ->（保持不发/可少量 MOVE）-> UP（时长由你控制）

> 长按本质不是独立协议类型，而是 **DOWN 与 UP 的时间间隔**。

---

## 4.2 滚轮：`INJECT_SCROLL_EVENT`（type=3）

固定 21 字节：

- type: 1B
- position(x,y,w,h): 12B
- hscroll: 2B（i16 定点）
- vscroll: 2B（i16 定点）
- buttons: 4B

注意：协议里滚动值按 `[-16, 16]` 语义编码。

---

## 4.3 按钮事件（电源/音量等）：`INJECT_KEYCODE`（type=0）

固定 14 字节：

- type: 1B
- action: 1B（DOWN=0，UP=1）
- keycode: 4B
- repeat: 4B
- metastate: 4B

常用 keycode：

- POWER = 26
- VOLUME_UP = 24
- VOLUME_DOWN = 25
- HOME = 3
- BACK = 4
- APP_SWITCH = 187

发送规则：

- 短按：DOWN + UP
- 长按键：DOWN（延时）UP

---

## 4.4 特殊：`BACK_OR_SCREEN_ON`（type=4）

2 字节：`type + action`。
jar 端逻辑是：
- 屏幕亮：当 BACK 处理
- 屏幕灭：仅 ACTION_DOWN 时会触发 POWER 点亮

---

## 5. Kotlin 协议编码器（可直接用）

## 5.1 基础写入工具（大端）

```kotlin
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer
import java.nio.ByteOrder

object Be {
    fun u8(v: Int) = byteArrayOf((v and 0xFF).toByte())
    fun i16(v: Int): ByteArray = ByteBuffer.allocate(2).order(ByteOrder.BIG_ENDIAN).putShort(v.toShort()).array()
    fun i32(v: Int): ByteArray = ByteBuffer.allocate(4).order(ByteOrder.BIG_ENDIAN).putInt(v).array()
    fun i64(v: Long): ByteArray = ByteBuffer.allocate(8).order(ByteOrder.BIG_ENDIAN).putLong(v).array()
}

private fun u16FixedPoint(f: Float): Int {
    val clamped = f.coerceIn(0f, 1f)
    return (clamped * 0xFFFF).toInt().coerceIn(0, 0xFFFF)
}

private fun i16FixedPointForScroll(value: Float): Int {
    // scrcpy scroll 语义范围 [-16,16]，先归一化到 [-1,1]
    val n = (value / 16f).coerceIn(-1f, 1f)
    // 对齐 scrcpy 的有符号16位定点语义
    val raw = (n * 0x7FFF).toInt().coerceIn(-0x8000, 0x7FFF)
    return raw and 0xFFFF
}
```

## 5.2 数据结构

```kotlin
data class DeviceFrameSize(val width: Int, val height: Int)
data class DevicePoint(val x: Int, val y: Int)
```

## 5.3 Touch 消息编码

```kotlin
object CtlType {
    const val INJECT_KEYCODE = 0
    const val INJECT_TOUCH_EVENT = 2
    const val INJECT_SCROLL_EVENT = 3
}

object MotionAction {
    const val DOWN = 0
    const val UP = 1
    const val MOVE = 2
}

fun encodeInjectTouch(
    action: Int,
    pointerId: Long,
    p: DevicePoint,
    frame: DeviceFrameSize,
    pressure: Float,
    actionButton: Int = 0,
    buttons: Int = 0,
): ByteArray {
    val out = ByteArrayOutputStream(32)
    out.write(Be.u8(CtlType.INJECT_TOUCH_EVENT))
    out.write(Be.u8(action))
    out.write(Be.i64(pointerId))
    out.write(Be.i32(p.x))
    out.write(Be.i32(p.y))
    out.write(Be.i16(frame.width))
    out.write(Be.i16(frame.height))
    out.write(Be.i16(u16FixedPoint(pressure)))
    out.write(Be.i32(actionButton))
    out.write(Be.i32(buttons))
    return out.toByteArray()
}
```

## 5.4 KeyCode 消息编码

```kotlin
object KeyAction {
    const val DOWN = 0
    const val UP = 1
}

object AndroidKeyCode {
    const val POWER = 26
    const val VOLUME_UP = 24
    const val VOLUME_DOWN = 25
    const val HOME = 3
    const val BACK = 4
}

fun encodeInjectKeycode(
    action: Int,
    keycode: Int,
    repeat: Int = 0,
    metastate: Int = 0,
): ByteArray {
    val out = ByteArrayOutputStream(14)
    out.write(Be.u8(CtlType.INJECT_KEYCODE))
    out.write(Be.u8(action))
    out.write(Be.i32(keycode))
    out.write(Be.i32(repeat))
    out.write(Be.i32(metastate))
    return out.toByteArray()
}
```

## 5.5 Scroll 消息编码

```kotlin
fun encodeInjectScroll(
    p: DevicePoint,
    frame: DeviceFrameSize,
    hscroll: Float,
    vscroll: Float,
    buttons: Int = 0,
): ByteArray {
    val out = ByteArrayOutputStream(21)
    out.write(Be.u8(CtlType.INJECT_SCROLL_EVENT))
    out.write(Be.i32(p.x))
    out.write(Be.i32(p.y))
    out.write(Be.i16(frame.width))
    out.write(Be.i16(frame.height))
    out.write(Be.i16(i16FixedPointForScroll(hscroll)))
    out.write(Be.i16(i16FixedPointForScroll(vscroll)))
    out.write(Be.i32(buttons))
    return out.toByteArray()
}
```

---

## 6. 渲染 View 坐标 -> 设备坐标（最关键）

你触摸到的是 `renderView` 坐标，不是设备坐标。必须做映射。

假设：
- 视频帧尺寸：`videoW x videoH`（例如 1080x2400）
- View 尺寸：`viewW x viewH`
- 渲染方式：`FIT_CENTER`（有黑边）

映射步骤：

1. `scale = min(viewW/videoW, viewH/videoH)`
2. `contentW = videoW * scale`, `contentH = videoH * scale`
3. `offsetX = (viewW - contentW)/2`, `offsetY = (viewH - contentH)/2`
4. 触点 `(vx, vy)` 先减 offset，落在内容区外则丢弃
5. `videoX = (vx - offsetX) / scale`, `videoY = (vy - offsetY) / scale`
6. 取整并 clamp 到 `[0, videoW-1]`、`[0, videoH-1]`
7. 协议里：
   - `x,y` 填上述坐标
   - `screenWidth,screenHeight` 填 **该事件对应的视频帧尺寸**（非常关键）

Kotlin 示例：

```kotlin
fun mapViewToVideo(
    vx: Float,
    vy: Float,
    viewW: Int,
    viewH: Int,
    videoW: Int,
    videoH: Int,
): DevicePoint? {
    val scale = minOf(viewW.toFloat() / videoW, viewH.toFloat() / videoH)
    val contentW = videoW * scale
    val contentH = videoH * scale
    val offsetX = (viewW - contentW) / 2f
    val offsetY = (viewH - contentH) / 2f

    val cx = vx - offsetX
    val cy = vy - offsetY
    if (cx < 0 || cy < 0 || cx >= contentW || cy >= contentH) return null

    val x = (cx / scale).toInt().coerceIn(0, videoW - 1)
    val y = (cy / scale).toInt().coerceIn(0, videoH - 1)
    return DevicePoint(x, y)
}
```

---

## 7. 触控手势到协议发送（点击/滑动/长按）

## 7.1 pointerId 约定

单指可固定 `pointerId = 0L`（或任意稳定值）。
多指时每个手指必须有不同 pointerId，并在其生命周期内保持不变。

## 7.2 事件发送器

```kotlin
import java.io.OutputStream
import java.util.concurrent.Executors

class ControlSender(private val os: OutputStream) {
    private val io = Executors.newSingleThreadExecutor()

    fun send(packet: ByteArray) {
        io.execute {
            os.write(packet)
            os.flush()
        }
    }

    fun close() {
        io.shutdown()
    }
}
```

## 7.3 在渲染 View 上处理触摸

```kotlin
fun onRenderTouch(event: android.view.MotionEvent, sender: ControlSender, frame: DeviceFrameSize, viewW: Int, viewH: Int) {
    val p = mapViewToVideo(event.x, event.y, viewW, viewH, frame.width, frame.height) ?: return
    val pointerId = 0L

    when (event.actionMasked) {
        android.view.MotionEvent.ACTION_DOWN -> {
            sender.send(encodeInjectTouch(MotionAction.DOWN, pointerId, p, frame, pressure = 1f))
        }
        android.view.MotionEvent.ACTION_MOVE -> {
            sender.send(encodeInjectTouch(MotionAction.MOVE, pointerId, p, frame, pressure = 1f))
        }
        android.view.MotionEvent.ACTION_UP,
        android.view.MotionEvent.ACTION_CANCEL -> {
            sender.send(encodeInjectTouch(MotionAction.UP, pointerId, p, frame, pressure = 0f))
        }
    }
}
```

### 长按实现

- 不需要特殊协议；只要 `DOWN` 后延迟较久再 `UP` 即可。
- 可配合 `GestureDetector` 判断 long-press 时机，但真正控制效果来自 DOWN/UP 时序。

---

## 8. 按钮事件（电源、音量等）发送模板

```kotlin
fun sendPower(sender: ControlSender) {
    sender.send(encodeInjectKeycode(KeyAction.DOWN, AndroidKeyCode.POWER))
    sender.send(encodeInjectKeycode(KeyAction.UP, AndroidKeyCode.POWER))
}

fun sendVolumeUp(sender: ControlSender) {
    sender.send(encodeInjectKeycode(KeyAction.DOWN, AndroidKeyCode.VOLUME_UP))
    sender.send(encodeInjectKeycode(KeyAction.UP, AndroidKeyCode.VOLUME_UP))
}

fun sendVolumeDown(sender: ControlSender) {
    sender.send(encodeInjectKeycode(KeyAction.DOWN, AndroidKeyCode.VOLUME_DOWN))
    sender.send(encodeInjectKeycode(KeyAction.UP, AndroidKeyCode.VOLUME_DOWN))
}
```

如果要“按住不放”音量键：
- 先发 DOWN
- 经过你设定时长后发 UP
- repeat 字段一般保持 0（scrcpy 常规路径也常为 0）

---

## 9. 多指/缩放补充

你若要完全复刻 scrcpy 的多指体验：

- 为每根手指维护 pointerId（`Long`）
- ACTION_POINTER_DOWN/UP 对应的协议 action 仍按 touch event 发
- jar 端会在 `Controller.injectTouch()` 内合成 pointer index 逻辑

简化版可先只做单指（点击/拖动/长按），再扩展多指。

---

## 10. 可靠性与时序建议

1. **单线程串行写 socket**：防止消息交错。  
2. **不要合并 DOWN/UP**：保持手势原始顺序。  
3. **MOVE 可限频**（例如 60Hz）：减轻带宽和注入压力。  
4. **尺寸变化立刻更新 frameSize**：旋转后坐标错配是最常见问题。  
5. **断线重连后重建 sender**：旧 OutputStream 不可复用。

---

## 11. 最小可用实现清单（Checklist）

- [ ] 建立 control socket，并拿到 OutputStream
- [ ] 实现 `encodeInjectTouch()`
- [ ] 实现 `encodeInjectKeycode()`
- [ ] 实现坐标映射 `mapViewToVideo()`
- [ ] RenderView touch 事件转 DOWN/MOVE/UP
- [ ] UI 按钮转 POWER/VOLUME keycode（DOWN+UP）
- [ ] 串行发送队列 + flush
- [ ] 旋转/分辨率变化时更新 frameSize

做到以上 8 点，你的 Kotlin App 在控制效果上就能与 scrcpy PC 端控制链路一致（视频音频已完成的前提下）。

---

## 12. 对照源码定位（快速查阅）

- PC 端序列化：`app/src/control_msg.c`
- PC 端触摸/鼠标消息构造：`app/src/mouse_sdk.c`
- PC 端按键与快捷键构造：`app/src/input_manager.c`
- PC 端控制发送线程：`app/src/controller.c`
- jar 端解析：`server/src/main/java/com/genymobile/scrcpy/control/ControlMessageReader.java`
- jar 端注入：`server/src/main/java/com/genymobile/scrcpy/control/Controller.java`
- 字节样例测试：`app/tests/test_control_msg_serialize.c`

