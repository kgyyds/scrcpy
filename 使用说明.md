# Scrcpy TCP 三端口连接使用说明

## 概述

本版本的 scrcpy 使用 TCP 三端口连接架构，PC端作为服务器监听三个独立端口，Android端作为客户端主动连接。这种架构支持稳定的数据传输，包括视频流、音频流和控制信号。

## 端口配置

- **视频端口**: 27183 (H.264/H.265/AV1 视频流)
- **音频端口**: 27184 (Opus/AAC/FLAC 音频流)
- **控制端口**: 27185 (双向控制消息)

## PC端启动命令

### 基本启动（使用默认端口）

```bash
# 在PC端运行
./scrcpy
```

### 指定监听地址

```bash
# 监听所有网络接口（推荐）
./scrcpy --listen-address 0.0.0.0

# 监听特定网络接口
./scrcpy --listen-address 192.168.1.11
```

### 自定义端口

```bash
# 使用自定义端口
./scrcpy --listen-video-port 50000 \
         --listen-audio-port 50001 \
         --listen-control-port 50002
```

### 完整命令示例

```bash
# 完整参数示例
./scrcpy --listen-address 192.168.1.11 \
         --listen-video-port 27183 \
         --listen-audio-port 27184 \
         --listen-control-port 27185 \
         --control \
         --audio \
         --show-touches \
         --stay-awake
```

## Android端JAR启动命令

### 准备工作

1. 将编译好的 JAR 文件推送到 Android 设备：
```bash
adb push scrcpy-server.jar /data/local/tmp/
```

2. 确保系统存在 app_process（Android 系统自带）
3. 进入 Android 设备 shell：
```bash
adb shell
```

### 基本连接

```bash
cd /data/local/tmp
CLASSPATH=/data/local/tmp/scrcpy-server.jar app_process / com.genymobile.scrcpy.Server \
    server_host=192.168.1.11 \
    connect_video_port=27183 \
    connect_audio_port=27184 \
    connect_control_port=27185
```

### 使用 Session ID

```bash
# 指定 Session ID（推荐）
CLASSPATH=/data/local/tmp/scrcpy-server.jar app_process / com.genymobile.scrcpy.Server \
    server_host=192.168.1.11 \
    connect_video_port=27183 \
    connect_audio_port=27184 \
    connect_control_port=27185 \
    scid=0x1
```

### 完整参数示例

```bash
# 完整参数示例
CLASSPATH=/data/local/tmp/scrcpy-server.jar app_process / com.genymobile.scrcpy.Server \
    server_host=192.168.1.11 \
    connect_video_port=27183 \
    connect_audio_port=27184 \
    connect_control_port=27185 \
    scid=0x1 \
    control=true \
    video=true \
    audio=true \
    send_dummy_byte=true \
    send_device_meta=true
```

### 简化命令（推荐使用）

创建脚本 `start-scrcpy.sh`：
```bash
#!/bin/bash
adb push scrcpy-server.jar /data/local/tmp/
adb shell "cd /data/local/tmp && \
           CLASSPATH=/data/local/tmp/scrcpy-server.jar \
           app_process / com.genymobile.scrcpy.Server \
           server_host=192.168.1.11 \
           connect_video_port=27183 \
           connect_audio_port=27184 \
           connect_control_port=27185 \
           scid=0x1"
```

### 使用方法

1. 先给脚本执行权限：
```bash
chmod +x start-scrcpy.sh
```

2. 运行脚本：
```bash
./start-scrcpy.sh
```

## 完整使用流程

### 步骤 1：启动 PC 端

```bash
# 在PC上运行，监听所有接口
./scrcpy --listen-address 0.0.0.0
```

PC 端会显示类似这样的信息：
```
[INFO] Listening on video port 27183
[INFO] Listening on audio port 27184
[INFO] Listening on control port 27185
[INFO] Waiting for device connection...
```

### 步骤 2：启动 Android 端

在 Android 设备上运行 JAR：

```bash
adb shell
cd /data/local/tmp
CLASSPATH=/data/local/tmp/scrcpy-server.jar app_process / com.genymobile.scrcpy.Server \
    server_host=192.168.1.11 \
    connect_video_port=27183 \
    connect_audio_port=27184 \
    connect_control_port=27185 \
    scid=0x1
```

### 步骤 3：建立连接

Android 端会尝试连接到 PC 的三个端口：
- 连接到 192.168.1.11:27183（视频）
- 连接到 192.168.1.11:27184（音频）
- 连接到 192.168.1.11:27185（控制）

连接成功后，PC端会显示：
```
[INFO] Device connected for video stream
[INFO] Device connected for audio stream
[INFO] Device connected for control stream
```

## 高级配置

### 1. 自定义 Session ID

为了避免冲突，建议使用自定义 Session ID：

```bash
# PC端
./scrcpy --scid 0x1234

# Android端
java -jar scrcpy-server.jar ... scid=0x1234
```

### 2. 网络配置

如果使用不同的网络，确保：

1. PC 和 Android 在同一网络或能够相互访问
2. PC 的防火墙允许端口 27183-27185 的入站连接
3. Android 能够解析 PC 的主机名或 IP 地址

### 3. 性能优化

```bash
# PC端优化
./scrcpy --max-fps 60 \
         --video-bit-rate 8M \
         --video-buffer 10

# Android端优化（通过JAR参数）
java -jar scrcpy-server.jar \
    video_bitrate=8000000 \
    max_fps=60 \
    video_buffer=10
```

## 故障排除

### 1. 连接失败

**问题**: Android 无法连接到 PC

**解决方案**:
- 检查 PC 的 IP 地址是否正确
- 确认防火墙允许端口连接
- 验证网络连通性：`ping 192.168.1.11`

### 2. 端口被占用

**问题**: 提示端口已在使用

**解决方案**:
- 使用不同的端口：
```bash
# PC端
./scrcpy --listen-video-port 50000 --listen-audio-port 50001 --listen-control-port 50002

# Android端
java -jar scrcpy-server.jar ... connect_video_port=50000 connect_audio_port=50001 connect_control_port=50002
```

### 3. 无音频/视频

**问题**: 连接成功但没有音频或视频

**解决方案**:
- 检查对应的端口参数是否一致
- 确认 JAR 参数中的 `video=true` 和 `audio=true`
- 查看 logcat 输出是否有错误信息

### 4. 控制不响应

**问题**: 鼠标键盘控制不响应

**解决方案**:
- 检查控制端口是否正确
- 确认 JAR 参数中的 `control=true`
- 查看 PC 端日志是否显示控制连接成功

## 日志查看

### PC端日志

PC端会直接在终端输出日志，包括：
- 监听端口信息
- 连接状态
- 错误信息

### Android端日志

可以通过 `logcat` 查看：
```bash
adb logcat | grep scrcpy
```

## 快速测试命令

### 测试环境搭建

```bash
# 1. PC端（192.168.1.11）
./scrcpy --listen-address 0.0.0.0 --listen-video-port 27183 --listen-audio-port 27184 --listen-control-port 27185

# 2. Android端
adb push scrcpy-server.jar /data/local/tmp/
adb shell
cd /data/local/tmp
java -jar scrcpy-server.jar server_host=192.168.1.11 connect_video_port=27183 connect_audio_port=27184 connect_control_port=27185
```

### 一键脚本

创建 `start-pc.sh`:
```bash
#!/bin/bash
./scrcpy --listen-address 0.0.0.0 --listen-video-port 27183 --listen-audio-port 27184 --listen-control-port 27185
```

创建 `start-android.sh`:
```bash
#!/bin/bash
adb push scrcpy-server.jar /data/local/tmp/
adb shell "cd /data/local/tmp && \
           CLASSPATH=/data/local/tmp/scrcpy-server.jar \
           app_process / com.genymobile.scrcpy.Server \
           server_host=192.168.1.11 \
           connect_video_port=27183 \
           connect_audio_port=27184 \
           connect_control_port=27185 \
           scid=0x1"
```

## 注意事项

1. **端口一致性**: PC 和 Android 必须使用相同的端口配置
2. **网络权限**: 确保设备间的网络连接没有被阻止
3. **资源清理**: 使用完毕后，按 Ctrl+C 退出程序
4. **版本匹配**: 确保 PC 端和 Android 端使用兼容的版本
5. **防火墙**: 如果使用 Windows，可能需要在防火墙中添加入站规则

## 总结

TCP 三端口连接架构提供了稳定可靠的屏幕镜像解决方案。通过合理配置端口和网络参数，可以实现高质量的远程控制和屏幕共享体验。